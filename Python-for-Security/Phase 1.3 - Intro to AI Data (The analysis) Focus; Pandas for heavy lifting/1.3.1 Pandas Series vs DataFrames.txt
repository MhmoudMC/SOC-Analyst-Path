How it works : 
	* pd.DataFrame(log_data) : Takes a massive, messy firewall log and breaks it into those neat, separate vertical lists (columns). 
	* df['Action']: Reaches into that master container and isolates only specific list you care about.
	* THE ADVANTAGE: Instead of writing a complex 10-line 'for' loop to scan a raw text file for blocked connections, you just pull the 'Action' list and analyze it instantly.

# Specifically, i should be able to :
	* Bypass Manual File Loops: Instead of using 'open()' and 'for' loops to read a test file line-by-line, i can load a 100k-row CSV log file into system memory using a single command   'df = pd.read_csv('logs.csv')' .

	* Isolate Specific Log Attributes: You can strip away unnecessary columns (like timestamps or file sizes) and grab only the single attribute pillar you car about as a Series: 
'ips = df['Source_IP']'

	* Prepare Data for Fast Math: You have shifted your data from a static text file on disk into a Pandas object in RAM, making it ready for instant statistical checks and filtering in the upcoming steps.

[ import pandas as pd

df = pd.read_csv('firewall_logs.csv') # Load the entire log table (DataFrame)
ip_list = df['Source_IP'}             # Isolate just the IP column (Series) 
 
