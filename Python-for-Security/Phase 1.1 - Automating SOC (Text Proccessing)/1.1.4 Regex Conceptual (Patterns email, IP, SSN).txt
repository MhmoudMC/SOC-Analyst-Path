1.1.3 & 1.1.4 the search & find due (i decided to relearn 1.1.3 since i cannot remember anything from yesterday) .
the os as how you walk into a room (file), and 're' (Regex) as how you find a specific needle (IP/Email) in a haystack of logs.

The SOC Logic:
*os: os.listdir() lets you see all the .log files in your server.
*re: re.findall() lets you pull out every IP address inside those files instantly.

Quick Regex Patterns for your "Speed run" :

Target      Regex Pattern              Simple Explanation

IP Address  \d{1,3}(\.\d{1,3}) {3}     1-3 numbers followed by a dot, repeated.

Email       [\w\.-]+@[\w\.-]           Words/dots, an @ sign, and more words.

SSN         \d{3}-\d{2}-\d{4}          The standard format for social security numbers.